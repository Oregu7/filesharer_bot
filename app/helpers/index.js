const compileMessage = require("./compileMessage");
const pagination = require("./pagination");
const fileManager = require("./fileManager");
const langManager = require("./langManager");
const startRouterDispatch = require("./startRouterDispatch");

module.exports = {
    compileMessage,
    pagination,
    fileManager,
    langManager,
    startRouterDispatch,
};